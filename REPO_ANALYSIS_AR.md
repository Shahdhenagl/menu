# تحليل مستودع `menu`

**تاريخ التحليل:** 13 أغسطس 2026  
**النسخة محل الفحص:** `12743c6555d82ddf117c44a7c65b69fbbe428fac`  
**الرابط:** [Shahdhenagl/menu](https://github.com/Shahdhenagl/menu)

## الخلاصة التنفيذية

المستودع عبارة عن تطبيق ويب أحادي الصفحة **SPA** لإدارة وتشغيل مطعم، مبني باستخدام **React 19 وTypeScript وVite**، مع طبقة بيانات تعتمد على **Supabase** عند توفر متغيرات البيئة، وتعود تلقائيًا إلى مخزن محلي داخل `localStorage` عند غيابها. التطبيق يجمع في قاعدة كود واحدة بين قائمة العملاء، سلة الطلبات، نقطة البيع POS، شاشة المطبخ، ولوحة الإدارة، إضافة إلى المخزون والمشتريات والتصنيع والحسابات والإقفالات والطباعة عبر QZ Tray وإشعارات Telegram.

البنية الحالية قابلة للتطوير وظيفيًا، لكنها ليست جاهزة للتوسع الآمن طويل المدى دون إعادة تنظيم. أكبر نقاط المخاطرة هي أن التوجيه والصلاحيات يتم التعامل معهما داخل الواجهة، وأن طبقة البيانات الكبيرة `src/lib/supabase.ts` تعمل كـ **God Object** وتخلط بين الاتصال بقاعدة البيانات، التخزين المحلي، قواعد العمل، والإشعارات. كما توجد 41 migration/SQL file تقريبًا، وعدة نسخ تاريخية من ملفات POS داخل الجذر، ما يجعل تحديد مصدر الحقيقة وتشغيل قاعدة بيانات جديدة أكثر صعوبة.

> **التقييم الهندسي:** المشروع يعمل ويُبنى بنجاح، لكنه يحتاج أولًا إلى تثبيت الأمان ومصدر الحقيقة، ثم فصل المجالات الوظيفية، ثم إضافة اختبارات ومراقبة قبل تنفيذ تغييرات كبيرة.

## خريطة النظام

| الطبقة | الملفات الرئيسية | المسؤولية الحالية |
|---|---|---|
| نقطة الدخول | `src/main.tsx`, `src/App.tsx` | تشغيل React، اختيار الشاشة اعتمادًا على `window.location.pathname`، تحميل البيانات العامة، إدارة اللغة والثيم والسلة والاشتراك اللحظي للطلبات. |
| واجهة العميل | `CustomerMenu.tsx`, `CartModal.tsx` | عرض المنيو، البحث/الفلاتر، إضافة المنتجات للسلة، وإرسال الطلبات. |
| التشغيل | `PosSystem.tsx`, `KitchenDashboard.tsx` | استقبال ومعالجة الطلبات، حالات المطبخ، الدفع، الطباعة، وعمليات POS. |
| الإدارة | `AdminDashboard.tsx` | حاوية إدارية كبيرة تتفرع إلى المنتجات والتصنيفات والإعدادات والموظفين والمخزون والمالية والشركاء والإقفالات. |
| المجالات المتخصصة | `DailyClosingView.tsx`, `ShiftClosingView.tsx`, `FinancialsView.tsx`, `Inventory*`, `PartnersView.tsx`, `ShiftRecordsView.tsx` | الحسابات، الإقفالات، المخزون، الموردون، الشركاء، وسجلات الورديات. |
| طبقة البيانات | `src/lib/supabase.ts`, `src/lib/warehouse.ts` | CRUD لـ Supabase، fallback محلي، بعض عمليات المخزون والإقفالات، وإطلاق إشعارات Telegram. |
| الخدمات | `src/utils/printUtils.ts`, `qzSecurity.ts`, `telegramUtils.ts`, `tax.ts`, `shiftClosing.ts` | الطباعة، توقيع QZ، الإشعارات، الضرائب، وحسابات الإقفال. |
| قاعدة البيانات | `supabase_schema.sql` وملفات `supabase_patch*.sql` | إنشاء الجداول وإضافة الأعمدة والسياسات والبيانات الابتدائية عبر سلسلة patches تراكمية. |

## التقنيات والاعتمادات

| المجال | الاختيار |
|---|---|
| Frontend | React 19 + TypeScript 6 + Vite 8 |
| البيانات | `@supabase/supabase-js`، مع fallback إلى `localStorage` |
| الواجهة والحركة | CSS مخصص، `framer-motion`، `lucide-react` |
| الرسوم والتقارير | `recharts` و`xlsx` |
| الطباعة | `qz-tray` مع توقيع WebCrypto اختياري عبر `VITE_QZ_CERT` و`VITE_QZ_KEY` |
| التكاملات | Telegram HTTP API، WhatsApp links، Supabase Realtime |
| النشر | Vercel configuration موجودة، والتطبيق المنشور ظاهر في About باسم `menu-five-navy.vercel.app` |

## تدفق التشغيل

عند تشغيل `App.tsx` يتم تحديد الشاشة من المسار: `/` للقائمة، `/admin` للإدارة، `/pos` لنقطة البيع، و`/kitchen` للمطبخ. بعد ذلك تُحمّل التصنيفات والمنتجات والطلبات والإعدادات بالتوازي من خلال `db.getCategories`, `db.getProducts`, `db.getOrders`, و`db.getSettings`. إذا لم توجد `VITE_SUPABASE_URL` أو `VITE_SUPABASE_ANON_KEY`، يتم إنشاء عميل Supabase بقيمة `null` وتستخدم وظائف البيانات مخزنًا محليًا بدلًا من قاعدة البيانات الحقيقية.

السلة محفوظة في `localStorage` تحت `meridien_cart`، واللغة والثيم محفوظان تحت مفاتيح منفصلة. الطلبات تستفيد من قناة Supabase Realtime على جدول `orders`، وبعد كل تغيير يعاد تحميل قائمة الطلبات بالكامل. هذا بسيط وفعّال في البداية، لكنه قد يصبح مكلفًا عند زيادة عدد الطلبات أو المستخدمين، كما أن الاشتراك الحالي لا يمثل بديلًا عن cache أو state management منظم.

## أهم ملاحظات الكود

### 1. طبقة البيانات كبيرة ومختلطة المسؤوليات

`src/lib/supabase.ts` يحتوي على ما يقارب 2,800 سطر ويضم أنواعًا كثيرة من العمليات: المنتجات والطلبات والمصروفات والموظفين والمخزون والموردين والتصنيع والشركاء والإقفالات والمعاملات المالية والإشعارات. كما يحتوي على fallback محلي وعمليات `localStorage` بجوار استدعاءات Supabase.

هذا يجعل أي تعديل في مجال واحد معرضًا لتأثيرات جانبية في مجال آخر. الترتيب الصحيح لاحقًا هو تقسيمها إلى وحدات مثل `ordersRepository`, `inventoryRepository`, `financeRepository`, `staffRepository`, و`settingsRepository`، مع طبقة use-cases مستقلة لقواعد العمل.

### 2. لوحة الإدارة هي أكبر نقطة تعقيد

`src/components/AdminDashboard.tsx` يتجاوز 10,000 سطر، وهو أكبر ملف في المشروع بفارق واضح. وجود كل tabs والمودالات والعمليات في ملف واحد يصعّب الاختبار والمراجعة ويزيد احتمالات regression. ينبغي تقسيمه حسب المجال، مع مكونات مشتركة للمودالات والجداول والفلاتر ورسائل التأكيد.

### 3. المصادقة والصلاحيات غير كافية كحاجز أمني

التطبيق يعتمد على مسارات داخلية وبيانات مستخدم محفوظة في `localStorage` مثل `meridien_logged_in_user` و`meridien_waiter`. هذا قد يغير مظهر الواجهة لكنه لا يوفر ضمانًا أمنيًا حقيقيًا؛ إذ يمكن للمستخدم تعديل `localStorage` أو استدعاء Supabase مباشرة من المتصفح. يجب نقل فرض الصلاحيات إلى Supabase Auth وRLS وسياسات مبنية على الدور، مع عدم الاعتماد على واجهة React وحدها.

### 4. سياسات قاعدة البيانات متساهلة جدًا

ملفات SQL تنشئ سياسات من نمط `FOR ALL USING (true) WITH CHECK (true)` على جداول عديدة. هذا يعني عمليًا السماح بالقراءة والإضافة والتعديل والحذف لأي عميل يملك مفتاح anon، وهو غير مناسب لنظام POS وبيانات مالية وموظفين. يجب اعتبار هذه أولوية P0 قبل أي إطلاق إنتاجي موسع.

### 5. Telegram يُستدعى من المتصفح

`telegramUtils.ts` يرسل الطلبات مباشرة إلى Telegram من الواجهة، ويحتوي على fallback token/chat IDs. أي token موجود في frontend bundle قابل للاستخراج من المتصفح؛ لذلك يجب إلغاء أي secret hardcoded فورًا ونقل الإرسال إلى Edge Function أو backend آمن، مع validation وrate limiting وعدم تمرير بيانات حساسة في النص الخام.

### 6. غياب فصل واضح بين مصدر البيانات الحقيقي والـ fallback

الـ fallback المحلي مفيد للتجربة والتشغيل دون إعداد، لكنه قد يخفي أخطاء الإنتاج ويعطي انطباعًا بأن الحفظ نجح بينما لم يُحفظ شيء في قاعدة البيانات. الأفضل وجود وضعين صريحين: `production` يفشل بوضوح إذا غاب Supabase، و`demo` يُفعّل عمدًا من خلال configuration، مع indicator ظاهر يوضح حالة الاتصال.

### 7. حجم الحزمة كبير

عملية build نجحت، لكن Vite أبلغ أن chunk JavaScript الرئيسي يقارب **2.1 MB قبل الضغط و567 KB gzip**، متجاوزًا حد التحذير الافتراضي 500 KB. السبب المرجح هو تحميل شاشات الإدارة وPOS والمطبخ وكل مكتباتها في الحزمة الأولى. الحل هو `React.lazy` وdynamic imports لكل مساحة، مع فصل مكتبات التقارير والطباعة والجداول قدر الإمكان.

### 8. الاختبارات الآلية غير ظاهرة

لا توجد بنية واضحة لاختبارات unit/integration/e2e أو CI workflow في الجذر المفحوص. قبل التعديلات الكبيرة ينبغي إضافة اختبارات لحساب الضرائب والإقفالات، انتقالات حالات الطلب، خصم المخزون، split payments، وصلاحيات الأدوار، ثم smoke test لتشغيل المسارات الرئيسية.

## حالة التحقق الحالية

| الفحص | النتيجة | الملاحظة |
|---|---|---|
| `npm install` | ناجح | تم تثبيت الاعتمادات محليًا. |
| `npm run build` | ناجح | `tsc -b` و`vite build` مرّا بنجاح. |
| تحليل bundle | تحذير | chunk رئيسي كبير ويحتاج code splitting. |
| `npm run lint` | لم يكتمل | العملية أُنهیت أثناء التنفيذ بسبب ضغط الذاكرة/الموارد، وليس بسبب خطأ lint مؤكد. يجب إعادة تشغيلها في بيئة موارد أفضل أو على مجلدات محددة. |
| حالة Git | نظيفة | لا توجد تعديلات محلية على النسخة المستنسخة. |

## ترتيب الأولويات المقترح

| الأولوية | الإجراء | السبب |
|---|---|---|
| P0 | إيقاف سياسات `FOR ALL true` واستبدالها بـ RLS مبني على Auth/roles | منع أي مستخدم من تعديل البيانات المالية أو حذف الطلبات والمخزون. |
| P0 | إزالة Telegram token وأي chat IDs افتراضية من frontend | منع تسريب الأسرار وإرسال رسائل غير مصرح بها. |
| P1 | توحيد migrations وإنشاء baseline واضح وقابل للتشغيل من قاعدة فارغة | منع اختلاف schema بين بيئات Vercel/Supabase المحلية والإنتاجية. |
| P1 | فصل `supabase.ts` إلى repositories وservices | تقليل الترابط وتسهيل اختبار قواعد العمل. |
| P1 | تقسيم `AdminDashboard.tsx` إلى وحدات مجال | تقليل مخاطر التعديل وتسريع التطوير. |
| P1 | إضافة Auth حقيقي وroute guards وpermission matrix | جعل الصلاحيات server-enforced وليست شكلية. |
| P2 | إضافة lazy loading وقياس bundle | تحسين زمن التحميل خصوصًا للعملاء على الهاتف. |
| P2 | إضافة اختبارات وقواعد lint/type strict تدريجيًا | منع regressions أثناء التعديلات القادمة. |
| P2 | إضافة logging ومراقبة أخطاء واضحة | تشخيص فشل الطباعة أو Supabase أو Telegram دون الاعتماد على console فقط. |

## خطة تنفيذ عملية للتعديلات القادمة

المرحلة الأولى يجب أن تكون **تثبيت الحالة الحالية**: إنشاء branch، توثيق متغيرات البيئة، تصوير schema الحالي، وتشغيل build وlint في بيئة نظيفة. بعدها نضيف اختبارات characterization للوظائف الحالية، خصوصًا الضرائب والإقفال وخصم المخزون، حتى لا نعيد تفسير السلوك الموجود أثناء refactor.

المرحلة الثانية هي **إصلاح الأمان ومصدر الحقيقة**. نفعّل Supabase Auth، نضع جدول أدوار/صلاحيات واضحًا، نكتب RLS policies منفصلة لكل جدول، ونحوّل العمليات الحساسة إلى RPC/Edge Functions عند الحاجة للذرية transactional behavior. في هذه المرحلة نوقف fallback الصامت في production وننقل Telegram إلى طرف خادمي.

المرحلة الثالثة هي **إعادة تنظيم الكود دون تغيير السلوك**. نفصل مكونات الإدارة حسب المجال، ثم نفصل repositories عن use-cases، ونعرّف أنواعًا دقيقة بدل `any` و`as any`. بعد كل مجموعة تغييرات نعيد build والاختبارات ونقارن النتائج مع السلوك الحالي.

المرحلة الرابعة هي **تحسين الأداء وتجربة التشغيل**. نستخدم lazy loading للشاشات، نخفف إعادة تحميل الطلبات الكاملة بعد Realtime event، نضيف pagination/filtering على الاستعلامات، ونراجع الطباعة QZ Tray باعتبارها integration مستقلة لها health status ورسائل فشل قابلة للفهم.

## قواعد العمل التي يجب حمايتها في أي تعديل

ينبغي التعامل مع الطلب ككيان له انتقالات حالات محددة بدل السماح بتغييرات عشوائية من أي شاشة. كذلك يجب أن يكون خصم المخزون والإقفالات والمعاملات المالية عملياتًا ذرية أو قابلة للتعويض، مع audit trail يحدد المستخدم والوقت والقيمة السابقة والجديدة. لا ينبغي السماح بتعديل السعر أو الكمية بعد إغلاق وردية دون تسجيل adjustment واضح.

كما يجب فصل بيانات العميل العامة عن بيانات التشغيل الداخلية. القائمة العامة تحتاج قراءة آمنة للمنتجات الظاهرة فقط، بينما POS والإدارة والمطبخ والمخزون تحتاج صلاحيات مختلفة. هذا الفصل يجب أن يظهر في schema وRLS وفي واجهات التطبيق معًا.

## قرار هندسي مبدئي

سأتعامل مع المستودع في التعديلات القادمة باعتباره **نظام POS/Restaurant Operations** وليس مجرد menu website. لذلك أي feature جديد سنحدده أولًا في المجال الذي ينتمي إليه، ثم نحدد أثره على schema والصلاحيات وقواعد العمل والطباعة والتقارير، وبعد ذلك ننفذه بأقل تغيير ممكن على السلوك القائم مع اختبار regression مناسب.

أفضل نقطة بداية عملية هي أن تحدد أول تعديل مطلوب، وسأربطه مباشرة بالملفات والجداول المتأثرة، ثم ننفذه على branch منظم مع مراجعة build والاختبارات بعد كل خطوة.

## المراجع

[1]: https://github.com/Shahdhenagl/menu "المستودع الرسمي menu على GitHub"
[2]: https://github.com/Shahdhenagl/menu/blob/main/src/App.tsx "نقطة دخول التطبيق App.tsx"
[3]: https://github.com/Shahdhenagl/menu/blob/main/src/lib/supabase.ts "طبقة البيانات Supabase"
[4]: https://github.com/Shahdhenagl/menu/blob/main/src/components/AdminDashboard.tsx "لوحة الإدارة"
[5]: https://github.com/Shahdhenagl/menu/blob/main/supabase_schema.sql "مخطط قاعدة البيانات الأساسي"
[6]: https://github.com/Shahdhenagl/menu/blob/main/src/utils/telegramUtils.ts "تكامل Telegram"
