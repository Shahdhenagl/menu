# إلغاء رسالة التحذير بتاعة QZ Tray قبل كل طباعة

## ليه بتظهر أصلاً؟

QZ Tray بيقسّم الطلبات لنوعين:

| النوع | السلوك |
|---|---|
| **موقّع** بشهادة QZ واثق فيها | بيطبع فورًا، من غير أي رسالة |
| **غير موقّع** (وضعنا الحالي) | بيوقف ويسأل قبل كل طباعة |

علامة **Remember this decision** بتشتغل مع الطلبات الموقّعة. مع الطلب غير الموقّع
مفيش هوية ثابتة يفتكرها QZ، فالسماح بيبقى مؤقت وبيروح مع أول قفل/فتح لـ QZ أو
جلسة متصفح جديدة — وعشان كده الرسالة بترجع كل شوية.

الحل النهائي: نعمل شهادة، نحطها جوه QZ، والموقع يوقّع كل طلب بالمفتاح الخاص.

---

## الخطوات (مرة واحدة بس)

### 1) ولّد الشهادة والمفتاح

محتاج `openssl` (موجود مع Git for Windows في `C:\Program Files\Git\usr\bin\openssl.exe`).

من Git Bash:

```bash
# مفتاح خاص + شهادة عامة صالحة 10 سنين
openssl req -x509 -newkey rsa:2048 -keyout private-key.pem -out digital-certificate.txt \
  -days 3650 -nodes -subj "/CN=Meridien POS/O=Meridien/C=EG"

# تحويل المفتاح لصيغة PKCS#8 (اللي المتصفح بيفهمها)
openssl pkcs8 -topk8 -inform PEM -outform PEM -nocrypt \
  -in private-key.pem -out private-key-pkcs8.pem
```

بيطلع عندك ملفين مهمين:
- `digital-certificate.txt` → الشهادة العامة (بتتحط في QZ)
- `private-key-pkcs8.pem` → المفتاح الخاص (بيتحط في إعدادات الموقع)

> **مهم:** الملفين دول **ما يترفعوش على GitHub**. سيبهم بره مجلد المشروع أو ضيفهم لـ `.gitignore`.

### 2) خلّي QZ يثق في الشهادة

على **كل جهاز** فيه QZ Tray:

1. انسخ `digital-certificate.txt` وسمّيه `override.crt`
2. حطّه في مجلد تنصيب QZ Tray:
   `C:\Program Files\QZ Tray\override.crt`
3. اقفل QZ Tray خالص من الـ tray واعمله Run as Administrator تاني

### 3) ظبّط المتغيرات على Vercel

من **Vercel → Project → Settings → Environment Variables** ضيف اتنين:

| الاسم | القيمة |
|---|---|
| `VITE_QZ_CERT` | محتوى `digital-certificate.txt` كله (بترويسة `-----BEGIN CERTIFICATE-----`) |
| `VITE_QZ_KEY` | محتوى `private-key-pkcs8.pem` كله (بترويسة `-----BEGIN PRIVATE KEY-----`) |

انسخ الملف بالكامل بأسطره زي ما هو. بعدها اعمل **Redeploy** للمشروع.

للتجربة محليًا: نفس المتغيرين في ملف `.env.local` جنب `package.json`.

### 4) جرّب

اطبع أي فاتورة — المفروض تتطبع على طول من غير أي رسالة.

---

## ملاحظة أمنية

المفتاح الخاص بينزل جوه ملفات الموقع في المتصفح، يعني مش سر مطلق. عمليًا ده معناه
إن حد يعرف اللينك ويكون **قاعد على جهاز فيه QZ Tray بالشهادة دي** يقدر يبعتله أوامر
طباعة. للاستخدام ده (أجهزة كاشير جوه المطعم) الوضع مقبول وده الأسلوب اللي QZ نفسها
بتوثّقه للمواقع اللي من غير سيرفر. لو حبيت أمان أعلى لاحقًا، التوقيع بيتنقل لسيرفر
صغير بيوقّع الطلبات والمفتاح ما يخرجش منه.

## من غير الخطوات دي؟

الطباعة شغالة عادي — بس رسالة التحذير هتفضل تظهر. الكود بيتأكد إن المتغيرين موجودين
قبل ما يفعّل التوقيع، فمفيش حاجة بتتكسر لو سيبتها.
